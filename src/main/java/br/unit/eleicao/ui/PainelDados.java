package br.unit.eleicao.ui;

/** Todo painel que exibe dados implementa esta interface e é avisado quando a base ou as posições mudam. */
public interface PainelDados {

    void dadosAtualizados(ContextoApp contexto);
}
